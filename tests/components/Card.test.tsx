import { describe, it, expect, vi } from 'vitest'; import { render, screen } from '@testing-library/react'; import Card from '../../components/UI/Card'; // Mock framer-motion vi.mock('framer-motion', () => ({ motion: { div: ({ children, ...props }: React.HTMLAttributes<HTMLElement>) => <div {...props}>{children}</div>, article: ({ children, ...props }: React.HTMLAttributes<HTMLElement>) => <article {...props}>{children}</article>, }, })); describe('Card Component', () => { it('renders children correctly', () => { render( <Card> <div data-testid="child">Test Content</div> </Card> ); expect(screen.getByTestId('child')).toBeInTheDocument(); }); it('applies custom className', () => { render( <Card className="custom-class"> <div>Content</div> </Card> ); const card = screen.getByRole('article') || screen.getByTestId('card'); if (card) { expect(card.className).toContain('custom-class'); } }); it('renders with title when provided', () => { render( <Card title="Test Card"> <div>Content</div> </Card> ); expect(screen.getByText('Test Card')).toBeInTheDocument(); }); it('renders with description when provided', () => { render( <Card description="Test Description"> <div>Content</div> </Card> ); expect(screen.getByText('Test Description')).toBeInTheDocument(); }); it('has proper accessibility attributes', () => { render( <Card> <div>Accessible Card</div> </Card> ); // Card should be accessible const card = screen.getByRole('article'); expect(card).toBeInTheDocument(); }); it('renders tags when provided', () => { const tags = ['React', 'TypeScript', 'Vitest']; render( <Card tags={tags}> <div>Content</div> </Card> ); tags.forEach(tag => { expect(screen.getByText(tag)).toBeInTheDocument(); }); }); it('handles hover state correctly', () => { render( <Card hoverable> <div>Hoverable Card</div> </Card> ); const card = screen.getByRole('article'); expect(card).toBeInTheDocument(); }); it('renders with different variants', () => { const { rerender } = render( <Card variant="default"> <div>Default</div> </Card> ); expect(screen.getByText('Default')).toBeInTheDocument(); rerender( <Card variant="featured"> <div>Featured</div> </Card> ); expect(screen.getByText('Featured')).toBeInTheDocument(); }); });